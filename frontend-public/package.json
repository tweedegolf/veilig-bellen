{
  "name": "frontend-public",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prepare": "NODE_ENV=production parcel build --no-source-maps -d dist --target browser --bundle-node-modules src/lib.mjs",
    "build-example": "NODE_ENV=production parcel build --no-source-maps -d dist --bundle-node-modules src/example.html"
  },
  "dependencies": {
    "@babel/polyfill": "^7.8.7",
    "@privacybydesign/irmajs": "^0.1.9",
    "axios": "^0.21.1",
    "babel-polyfill": "^6.26.0",
    "preact": "^10.4.1"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "sass": "^1.26.2"
  },
  "targets": {
    "main": {
      "engines": {
        "browsers": [
          "> 1%",
          "not dead"
        ]
      }
    }
  }
}
